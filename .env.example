# 代理地址（TPS 模式，每次请求自动换 IP）
PROXY_URL=http://user:pwd@host:port

# 服务器端口
SERVER_PORT=8899

# 配送邮编
DEFAULT_ZIP_CODE=10001

# catalog_sync 增量导出的共享密钥（X-Export-Token 请求头）。
# 服务器是公网 IP：**没配就是关闭导出（503）**，不是放行。
EXPORT_TOKEN=
