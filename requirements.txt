# Server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
jinja2>=3.1.2
python-multipart>=0.0.6
aiofiles>=23.2.1
aiosqlite>=0.19.0
openpyxl>=3.1.2

# Worker
curl_cffi>=0.7.0
selectolax>=0.3.21
lxml>=5.1.0
dateparser>=1.2.0

# Screenshot
playwright>=1.40.0

# Shared
python-dotenv>=1.0.0
# 内存管理：Chromium RSS 封顶 + MEM_DIAG 诊断需要读进程树 RSS（跨平台）。
# 缺失时不报错，仅降级为"按渲染张数重启浏览器 + 无 RSS 诊断"。
psutil>=5.9.0
